{
  "name": "@boxfox/router",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=24.0.0" },
  "scripts": {
    "dev": "node src/main.mjs",
    "start": "node src/main.mjs --production",
    "test": "node --test tests/*.test.mjs",
    "test:live": "node scripts/live-test.mjs",
    "check": "node --check src/server.mjs && node --check src/engine.mjs"
  }
}
